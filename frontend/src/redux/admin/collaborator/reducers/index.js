import collaboratorsList from "./collaboratorList.reducer";
import collaborator from "./collaborator.reducer";

const CollaboratorReducer = {
  collaboratorsList,
  collaborator,
};

export default CollaboratorReducer;
