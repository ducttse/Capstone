import transactionsList from "./transactionsList.reducer";
import transaction from "./transaction.reducer";

const TransactionReducer = {
  transactionsList,
  transaction,
};

export default TransactionReducer;
